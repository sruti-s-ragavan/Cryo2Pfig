#!/usr/bin/python
import os
import datetime
def main():
	a_dir = "."
	folders = [name for name in os.listdir(a_dir) if os.path.isdir(os.path.join(a_dir, name))]
	print folders
	new_list = []
	for folder in folders:
		for file in folder:
			print file
if __name__ == '__main__':
	main()
